<h1>My List</h1>

<% @tasks.each do |task| %>
    <li><%=link_to task.title, task_path(task.id)%><%= link_to '✖', task_path(task.id), method: :delete, data: {confirm: 'Are you sure?'} %></li>
<% end %>

<%= link_to "+ Add a new task", tasks_new_path%>
